services:
  weekly-scorer:
    build: .
    container_name: weekly-scorer
    restart: unless-stopped
    ports:
      - "8081:8000"
    volumes:
      - ./uploads:/app/uploads
    env_file:
      - .env
    environment:
      - TZ=Asia/Shanghai
    extra_hosts:
      - "host.docker.internal:host-gateway"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
