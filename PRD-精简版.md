# 周报评分系统 v2 - 产品需求文档（精简版）

## 一、项目概述

基于 AI 的智能周报评估平台，通过自动化评分机制提升团队周报质量，为管理者提供数据化的团队工作表现洞察。

### 项目目标
- 效率提升：自动评分替代人工评审，节省 80% 的周报审核时间
- 质量提升：通过多维度评分引导员工产出高质量周报
- 数据洞察：提供周报质量趋势分析、团队排行榜等数据可视化
- 灵活配置：支持自定义评分维度、权重和等级标准
- 降级兜底：AI 服务不可用时自动切换为规则评分

---

## 二、核心功能

### 1. 管理配置
- 评分维度管理：添加/编辑/删除维度，设置满分、最高分、最低分、考核内容
- 等级阈值配置：优/良/一般/差分数阈值
- AI Prompt 自定义：调整评分提示词
- 数据状态监控：查看部门数、人员数、报告数
- 数据库备份：手动触发备份

### 2. 模板管理
- 模板 CRUD：创建、查看、编辑、删除周报模板
- 默认模板设置
- 表单字段定义：支持 text/textarea 类型

### 3. 周报管理
- 文件上传：支持 Excel(.xlsx/.xls)、Word(.docx)、PDF 格式
- 在线编辑：Markdown 编辑器
- 草稿保存
- 周报提交：自动触发 AI 评分
- 周报分类：自动识别本周/补周报/未知时间

### 4. AI 评分引擎
- 多 AI 支持：DeepSeek/豆包/MiMo API（OpenAI 兼容）
- 自动降级：AI 服务异常时切换为规则评分
- 评分约束：维度分差不超过 22 分

### 5. 排行榜
- 周期筛选：本周/本月/全部
- 部门筛选：按部门查看
- 统计分析：总报告数、已评分数、平均分、等级分布
- 趋势图表：最近 12 周评分趋势

### 6. 部门/人员管理
- 部门信息管理
- 人员信息管理

---

## 三、系统架构

### 技术栈
- **前端**：Vue 3 + PrimeVue 4 (Aura) + ECharts 5
- **后端**：FastAPI + SQLAlchemy (async) + SQLite
- **AI**：DeepSeek/豆包/MiMo API（OpenAI 兼容）

### 四层架构
1. **前端层**：6个页面（Dashboard、Config、WriteReport、ReportList、ReportDetail、Leaderboard）
2. **后端层**：FastAPI 应用，包含 6 个 API 路由模块
3. **数据层**：SQLite 数据库，6 个数据表
4. **AI 服务层**：外部 AI API 调用

---

## 四、数据模型

### 核心数据表

| 表名 | 说明 |
|------|------|
| `departments` | 部门信息 |
| `persons` | 人员信息 |
| `scoring_configs` | 评分配置 |
| `report_templates` | 周报模板 |
| `weekly_reports` | 周报记录 |
| `report_scores` | 评分结果 |

---

## 五、业务逻辑

### 评分流程

```
用户提交周报 → 检查内容长度 → 获取评分配置 → 调用AI评分 → 应用评分约束
    ↓
计算总分和等级 → 保存评分结果 → 更新周报状态
```

* AI 失败时自动降级为规则评分

### 评分规则

#### AI 评分
- 各维度按指定满分评分
- 总分 = 各维度分数直接相加
- 等级标准：优(≥45)、良(≥38)、一般(≥33)、差(<28)

#### 规则评分（降级策略）
- 基础分 = max(总分×0.4, 总分×0.5 + 内容长度÷40)，上限为总分×0.9
- 各维度按权重分配基础分，加入随机波动(-3~+3)

### 周报分类逻辑
| 条件 | 分类 |
|------|------|
| week_diff == 0 | 本周周报（normal） |
| week_diff > 0 | 补周报（catch_up） |
| week_diff < 0 | 未来周报（拒绝） |
| 日期无法识别 | 未知时间（unknown） |

---

## 六、使用方式

### 快速启动

**后端**
```bash
cd backend
python -m venv venv
venv/Scripts/activate
pip install -r requirements.txt
cp .env.example .env  # 填入 AI API Key
python -m uvicorn app.main:app --host 0.0.0.0 --port 8002 --reload
```

**前端**
```bash
cd frontend
npm install
npm run dev
```

访问 http://localhost:3001

API 文档：http://localhost:8002/docs

### 主要页面
- **Dashboard**：数据概览、统计图表
- **Config**：评分配置管理
- **WriteReport**：周报撰写/上传
- **ReportList**：周报列表
- **ReportDetail**：周报详情和评分结果
- **Leaderboard**：排行榜和统计分析

---

## 七、默认配置

### 默认评分维度

| 维度名称 | 满分 | 考核内容 |
|----------|------|----------|
| 工作反馈深度 | 14 | 问题发现+分析+解决方案 |
| 进度节点明确 | 13 | 项目是否有明确进度/节点 |
| 计划可行性 | 10 | 下周计划是否具体可执行 |
| 工作连续性 | 13 | 是否承接上周计划且有闭环 |

### 默认等级阈值

| 等级 | 分数阈值 |
|------|----------|
| 优 | ≥45 |
| 良 | ≥38 |
| 一般 | ≥33 |
| 差 | &lt;28 |

---

**文档版本**：v1.0（精简版）
**创建日期**：2026-06-05